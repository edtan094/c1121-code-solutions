/* exported setValue */
// function: setValue
// argument: an object
//            a string
//           a value
//  return: function does not return anything

//  take the object given and put the key with the value inside the object

function setValue(object, key, value) {
  object[key] = value;
}
